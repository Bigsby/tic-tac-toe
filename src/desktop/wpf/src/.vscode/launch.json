{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Full .Net Debug",
            "type": "clr",
            "request": "launch",
            "program": "${workspaceRoot}/bin/Debug/tic-tac-toe.exe",
            "args": [],
            "cwd": "${workspaceRoot}",
            "stopAtEntry": false,
            "console": "internalConsole",
            "preLaunchTask": "build"
        }
    ]
}